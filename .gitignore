# Environment files (secrets)
.env
.env.bak.*

# Local-only notes with secrets (install/deploy details) — never commit
*.local.md

# SDD/OpenSpec planning — not versioned in this infra repo
openspec/

# IDE
.idea/

# Playwright / test artifacts
.playwright-mcp/
*.png
